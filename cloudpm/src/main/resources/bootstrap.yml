eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
security:
  user:
    name: cloud-name
    password: cloud-pswd

spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: cloud-config
      profile: dev
      label: master